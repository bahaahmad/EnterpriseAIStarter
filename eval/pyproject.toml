[project]
name = "kb-eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "pyyaml>=6", "python-dotenv>=1"]

[project.optional-dependencies]
# REQ-EVAL-03: add once a Ragas version is pinned and mirrored internally
ragas = ["ragas", "datasets", "langchain-openai"]
